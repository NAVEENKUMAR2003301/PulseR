[build]
 
base = "client"        # correct (relative, quoted)
publish = "dist"
command = "npm run build"
[build.environment]
  # optional: example to set Node version via environment
  NODE_VERSION = "16"